<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mysite.dao.UserDao">
    <sql id="BASE_TABLE">
        t_users
    </sql>

    <sql id="BASE_COLUMN">
        uid,username,password,email,home_url,screen_name,created,activated,logged,group_name
    </sql>

    <update id="updateUserInfo" parameterType="com.mysite.model.entity.User">
        UPDATE
          <include refid="BASE_TABLE"/>
        <set>
            <if test="password != null">
                password = #{password, jdbcType=VARCHAR},
            </if>
            <if test="screenName != null">
                screen_name = #{screenName, jdbcType=VARCHAR},
            </if>
            <if test="email != null">
                email = #{email, jdbcType=VARCHAR},
            </if>
        </set>
        WHERE uid = #{uid, jdbcType=INTEGER}
    </update>

    <select id="getUserInfoById" resultType="com.mysite.model.entity.User">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
        WHERE
        uid = #{uid, jdbcType=INTEGER}
    </select>

    <select id="getUserInfoByCond" resultType="com.mysite.model.entity.User">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
        <where>
            <if test="username != null">
                AND username = #{username, jdbcType=VARCHAR}
            </if>
            <if test="password != null">
                AND password = #{password, jdbcType=VARCHAR}
            </if>
        </where>
    </select>


</mapper>